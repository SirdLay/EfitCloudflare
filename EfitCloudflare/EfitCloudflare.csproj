<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net6.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <ApplicationIcon>Cloudflare_Logo.ico</ApplicationIcon>
  </PropertyGroup>

  <ItemGroup>
    <Content Include="Cloudflare_Logo.ico" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="IpSet" Version="0.1.1" />
  </ItemGroup>

</Project>
